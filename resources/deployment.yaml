apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kubernetes-monitor
  name: k8s-monitor
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: load-k8s-monitor
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 50%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      creationTimestamp: null
      labels:
        app: load-k8s-monitor
    spec:
      containers:
        - name: test
          image: gcr.io/codefresh-inc/codefresh/pipeline-manager:3.80.10-saas-4080
